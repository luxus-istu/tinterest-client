services:
  tinterest-client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Placeholder for the Spring Boot backend
  # tinterest-server:
  #   image: tinterest-server:latest
  #   ports:
  #     - "8080:8080"
